const pRoot = '/'
const pCate = `${pRoot}category/`
const pCateSeries = `${pCate}series/`
const pCateBase = `${pCate}base/`
const pCateEnvConfig = `${pCate}env_config/`

const pLife = `${pRoot}life/`
const pTag = `${pRoot}tag/`
const pTimeline = `${pRoot}timeline/`
const pAbout = `${pRoot}about/`

module.exports = {
  pRoot,
  pCate,
  pCateSeries,
  pCateBase,
  pCateEnvConfig,
  pLife,
  pTag,
  pTimeline,
  pAbout,
}
